# Implementação recursiva com depuração de código.

Projeto acadêmico em Java que utiliza **recursividade** para preencher e exibir os elementos de um vetor. O objetivo é praticar conceitos de **recursão** e manipulação de arrays de forma controlada.

